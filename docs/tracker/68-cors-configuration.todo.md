# 68: CORS Configuration

## Description
Add CORS middleware to FastAPI so the web app can talk to the backend from a different origin. Configurable allowed origins via env var.

## Dependencies
- Depends on: #01 (FastAPI app)
