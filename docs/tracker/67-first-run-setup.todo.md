# 67: First-Run Setup / Database Seeding

## Description
Create default workspace + admin user on first boot. Detect empty database, seed initial data, show setup instructions.

## Dependencies
- Depends on: #59a (user model), #47 (workspace API)
