# Issue #90: Add dark theme to mobile app

## Problem

The mobile app currently uses a light theme only. Add a dark theme option.

## Requirements

- [ ] Add dark mode color scheme to React Native styles
- [ ] Theme toggle in settings screen
- [ ] Persist theme preference in AsyncStorage
- [ ] Respect system preference (`useColorScheme()`) as default
- [ ] All screens and components should support both themes

## Scope

Mobile app only (`mobile/` directory).
