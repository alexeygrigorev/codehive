# 59: Auth and Multi-User (Parent)

## Description
User authentication (JWT), user management, per-user workspaces, permission system (who can access which projects/sessions). Split into three sub-issues.

## Sub-Issues
- **59a** -- User model + JWT authentication (backend)
- **59b** -- Permissions and authorization (backend)
- **59c** -- Login UI + auth flow (frontend)

## Dependencies
- Depends on: #01 (FastAPI), #03 (DB models), #47 (workspace API)
